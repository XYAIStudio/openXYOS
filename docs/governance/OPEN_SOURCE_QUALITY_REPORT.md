# openXYOS 开源候选质量报告

> 版本：0.6.0 候选基线。此报告记录公开候选的工程检查范围，不构成法律意见、专利检索结论或对未知历史材料的保证。

## 已完成的公开边界处置

- 已移出专有治理方法相关的源码、路由、数据迁移、测试、演示页面和历史研发规格。
- 公共版群聊仅保留独立建议、会议纪要草稿和人工确认；不包含专有自动决策或专有流程。
- 公共版治理引擎使用独立的通用权限、通信规则、流程模板和审计事件实现。
- 发布扫描会拦截密钥样式、私有包引用、运行时数据库/备份/证书/归档文件，以及保密方法标识。

## 必须通过的命令

```text
npm run lint
npm run typecheck
npm test
npm run verify:open-source
npm run build
npm audit --registry=https://registry.npmjs.org --audit-level=high
```

## 发布前仍需人工确认

- 以干净目录执行 `npm ci` 后重复上述命令。
- 复核所有演示数据、图片、字体、商标和第三方素材的授权。
- 将完整资产台账、合同、技术交底、专利对比材料、申请材料、客户资料、生产配置与发布证据保存在私有系统，不提交仓库。
- 首发标签、GitHub 仓库可见性、分支保护、CLA/CCLA 门禁及安全联系渠道完成配置后再发布。

## 当前结论

2026-09-12 已在本候选目录完成 `npm run ci`（静态检查、类型检查、全套测试、公开边界扫描和生产构建）与独立复核；均通过。保密方法标识扫描仅命中检查器自身的规则清单，密钥样式及私有包引用扫描无命中；本地测试数据库受 `.gitignore` 排除。

依赖审计已通过兼容版本覆盖消除 `qs` 问题。当前仍有 2 项中危 `uuid` 告警，均来自 `exceljs` 及项目当前 UUID 主版本；审计工具给出的唯一自动修复方案为升级至 `uuid@14` 的破坏性变更，未在未经专项兼容性验证的情况下强制执行。发布前应跟踪上游修复或完成该升级的专门回归；当前不存在高危或严重级别告警。

通过自动扫描与工程验证，不等于完成商标、著作权、专利或合同法律审核。任何新增代码、文档、构建产物和附件都必须再次经过发布闸门。
